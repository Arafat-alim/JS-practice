// ! GLobal scope which created just outside the function scope
var scope = 1;

// ! function Scope which created just inside the function scope
function scoping() {
  var sco = 25;
  console.log(sco);
}

//! Two way to decalre a class

const c = new Circle(); //we cont create circle object like this. because it is not define
//! Class declaration (not support hoisitng)
class Circle {
  //dont support any hoisting concept
}
//! Class expression (We programmer dont use this much)3
const Square = class Sq {};

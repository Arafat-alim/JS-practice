function btnClicked() {
  const element = document.querySelector(".a"); //first element matches
  console.log(element);
  const elementa = document.querySelectorAll(".a");
  console.log(elementa); //all matches element show here
}

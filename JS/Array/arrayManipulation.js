//Array manipulation - insert and delete

let arr = ["one", "two", "three"];

//! push() add element at the end.
/* console.log(arr.push("Test")); // it return the updated length of an array
console.log(arr); // updated the array */
//! unshift() add element at the begining
/* console.log(arr.unshift("Test")); //it return the updated length of an array - 4
console.log(arr); // updated the array - Array(4) [ "Test", "one", "two", "three" ] */

//! pop() removed element at the end
/* console.log(arr.pop()); // it return the deleted item - three
console.log(arr); //updated the array - Array [ "one", "two" ] */

//! shift() removed element at the begining
/* console.log(arr.shift()); // it return the deleted item - one
console.log(arr); //Array [ "two", "three" ] */

//! splice method - which can delete or add my elemtn to
